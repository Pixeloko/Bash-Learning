## Operators to check on files and diretories
### Display a boolean

`-d` is it a directory
`-e` is it a file + `-f`is it regular, `-r`is it readable
`-s` is file's size < 0
`-w` is it writable
`-x` is it executable
